Place the native FFmpeg executables used by the browser-mode backend in this folder:

- Windows: `ffmpeg.exe`
- macOS/Linux: `ffmpeg`

The batch launcher passes `ffprobe.exe` from this folder to the Python backend. Choose and distribute an FFmpeg build whose license terms fit the application before shipping.
